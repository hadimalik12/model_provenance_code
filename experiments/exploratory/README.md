# Exploratory Experiments

Use this area for work that is useful but does not correspond directly to a
paper table. Each exploratory experiment must have its own README, config, and
named purpose. Do not use generic names such as `test`, `new`, or `table4`.

The nonmember control report lives in `nonmember_controls/` because it is a
cross-cutting validity check rather than a paper table on its own.
