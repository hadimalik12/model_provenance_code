experiment:
  id: table_03_parent_signal
  purpose: Base Pythia MIMIR GitHub membership-signal sanity check
  variant: augmented
dataset:
  source: iamgroot42/mimir
  config: github
  control: nonmember_vs_nonmember
scoring:
  method: domain_text_augmentation
  n_augmentations: 4
  primary_score: min_k_20_logprob
outputs:
  run_id: augmented_seed0
